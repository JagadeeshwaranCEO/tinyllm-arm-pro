# TinyLLM-ARM-Pro | Python Dependencies
# Pinned for reproducibility — ARM64 Apple Silicon
# Install: pip install -r requirements.txt

# GGUF inference via llama.cpp (built from source on ARM64)
llama-cpp-python>=0.3.29

# PyTorch with Metal GPU support
torch>=2.12.0

# Tokenizer (for baseline.py academic perplexity)
transformers>=4.40.0

# System monitoring (RAM, CPU detection)
psutil>=6.0.0
